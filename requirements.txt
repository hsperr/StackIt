# Runtime dependencies for the StackIt web server.
#
# torch is only needed to offer AlphaZero as an opponent; AlphaBeta and MCTS
# are pure Python. On a server install the CPU-only build, or pip pulls in the
# CUDA wheels and several gigabytes with them:
#
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
#
# See DEPLOY.md.

flask>=3.0
gunicorn>=21.0
numpy>=1.24
torch>=2.0
